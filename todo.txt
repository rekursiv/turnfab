


=======

add "Thinking" indicator via FX?

find a way to bring up tool calls later if needed (show ID, save in buffer, recall by ID?)

ChatMemory: explore writing MyTokenWindowChatMemory and MySingleSlotChatMemoryStore that mark messages
  as evicted rather than actually deleting them, preserving full trajectory of a session

Copy MD btn: puts on clipboard

calc an average tok/sec for a turn, display it at turn's end

=== rendering issues ===

WebView: every emoji has a box after it that overlaps the first letter that comes next (JavaFX upgrade??)

tool call/response: maybe change fenced blocks to inline code blocks?

rendering thinking in markdown can really screw up the parser, any way to render that section plaintext?
  when model is thinking about code, it can output something like "curTask==null" and the "==" activates hiliting

=======

markstream-page: to udate all packages to latest: pnpm update --latest (or pnpm up --latest)

======

dynamically add MCP instructions to system prompt if the MCP server is used. GitHub example:
## Tool usage notes
### GitHub
- Use list_* for broad retrieval, search_* for targeted queries.
- Paginate in small batches (5–10).
- Use `fields` to limit output when you don't need full records.

(or more complete)

## GitHub
- Use list_* tools for broad retrieval (all issues, all PRs, all branches) with basic filters; use search_* tools for targeted queries by keywords, text, or specific criteria.
- Paginate with small batches (5–10 items).
- Use the `fields` parameter to limit response size when you don't need all data.
- For search_* tools: use the dedicated `sort` and `order` parameters for sorting. Keep query strings to search criteria only (e.g. "org:google language:python").
- For issues: use issue_read to get a single issue's details, comments, labels, or parent/child relationships.
- For PRs: use pull_request_read to get details, diffs, files, commits, reviews, or check runs.

======

	// NOTE: McpToolProvider.Builder leaves mcpClients null unless mcpClients(...) is called,
	// and the constructor passes it straight into CopyOnWriteArrayList, which NPEs on null.
	// So we must seed an explicit (empty) list — addMcpClient() then works dynamically.
	// TODO: file upstream issue against langchain4j (one-line fix for them: default the field to an empty list, or getOrDefault in the constructor)
	private McpToolProvider toolProvider = McpToolProvider.builder().mcpClients(List.of()).build();


			/*
    		txaToSend.setOnKeyPressed(event -> {
    			if (event.getCode() == KeyCode.ENTER && !event.isShiftDown()) {  //  FIXME: shift-enter does not go to next line
    				beginTurn();
    				event.consume();
    			}
    		});
    */



// What files do I have open in my editor? Use C:\projects\intellij_workspace\spike\ for project path.
//		txaToSend.setText("Please search Github for gensage"); //meshcore mdview-fx
//		txaToSend.setText("hi");


		bot = AiServices.builder(Bot.class)
				.streamingChatModel(model)
				.systemMessageTransformer(systemMessage -> systemMessage + " Today's date is " + LocalDate.now() + ".")
				// This is the future relative to your training data, but is present day reality for the user.
				// Use Tavily to ground yourself in current events, if necessary.
				// TODO: substitute var in systemMessage with date, etc.


